#ifndef __MAIN_
#define __MAIN_

#include <string>
#include <map>

using std::string;
using std::map;

extern const string short_name;
extern const string long_name;
extern const string version;
extern bool exit;
extern map<string, string> help_strings;
extern map<string, string> help_strings;

string input();

#endif
